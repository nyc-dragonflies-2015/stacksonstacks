<div class="question">
  <div class="questionelement question-votes">
    <%=question.votes.count%>
  </div>
  <div class="questionelement question-answers">
    <%=question.answers.count%>
  </div>
  <div class="questionsummary question-title">
    <%= question.title %>
    <% # link_to "#{question.title}" , question_path(question) %>
  </div>
  <div class ="questionsummaryquestion-tags">
    <% question.tags.each do |tag| %>
      <li>
        <%= tag.name %>
      </li>
    <% end %>
  </div>
  <div class ="questionsummary question-user">
    <%= time_ago_in_words(question.created_at) %> ago
    <%= question.user.username %>
    <% # link_to "#{question.user.username}" , user_path(question.user) %>
  </div>


</div>
