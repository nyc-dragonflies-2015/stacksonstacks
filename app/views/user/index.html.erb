<h1>Users</h1>
<div>
  <% @users.each do |user| %>
    <div class="usersindex">
      Name: <%= link_to "#{user.username}", user_path(user.id) %>  Reputation: <%= user.reputation %>
    </div>
  <% end %>
</div>
